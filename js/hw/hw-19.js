"use strict"

//Отримати масив імен всіх користувачів (поле name).

let names = []

const addName = (users) =>{
    names.push(users.names

    )
} 

//Отримати масив об'єктів користувачів за кольором очей (поле eyeColor).

let eyeColorArr = []

const addEyeColor = (users) =>{
    names.push(users.eyeColor

    )
} 


//Отримати масив імен користувачів за статтю (поле gender).

let namesGenderMan = []
let namesGenderWoman = [] 

const addNameGender = (users) => {
    if (users.gender = "man") {
        namesGenderMan.push(users.names

        )
    } 
    else{
        if (users.gender = "man") {
            namesGenderWoman.push(users.names
    
            )
    }
}
}

//Отримати масив тільки неактивних користувачів (поле isActive).
let active = []


const checkActive = (users) => {
    if (users.isActive = true) {
        active.push(users.names

        )
    } 

}

//Отримати користувача (не масив) по email (поле email, він унікальний).

const email = dtweoiwu
let user1 = ""

const findUser = (users) => {
    if (users.email = email) {
        user1 = users.names

        
    } 

}


